pub struct RunnerManager {
    // runners: HashMap<ScriptType, Box<dyn Runner>>,
}

impl RunnerManager {
    pub fn new() -> Self {
        Self {
            // runners: HashMap::new(),
        }
    }
}

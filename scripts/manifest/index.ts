import { CHIMERA_CLIENT_MANIFEST } from './chimera-client';
import { CLASH_META_MANIFEST } from './clash-meta';
import { CLASH_RS_MANIFEST } from './clash-rs';

export const clashManifest = {
  meta: CLASH_META_MANIFEST,
  rs: CLASH_RS_MANIFEST,
  chimeraClient: CHIMERA_CLIENT_MANIFEST,
};
